[package]
name        = "livetest"
version.workspace      = true
publish     = false
edition.workspace      = true
license.workspace      = true
repository.workspace   = true
rust-version.workspace = true

[[bin]]
name = "livetest"
path = "src/main.rs"

[dependencies]
# `oidc` on: this crate exists to point the library at real clusters, and an
# OAuth-secured listener needs a real issuer. Not published, so the feature
# costs a downstream nothing.
kafka-conn  = { path = "../kafka-conn", features = ["oidc"] }
kafka-meta  = { path = "../kafka-meta" }
kafka-admin = { path = "../kafka-admin" }
kafka-read  = { path = "../kafka-read" }
kafka-consume  = { path = "../kafka-consume" }
kafka-produce = { path = "../kafka-produce" }
testkit     = { path = "../testkit" }

tokio              = { workspace = true }
futures            = { workspace = true }
bytes              = { workspace = true }
anyhow             = { workspace = true }
tracing            = { workspace = true }
tracing-subscriber = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
